# PT-GOLF — 자기완결형 에이전트 핸드오프

## 최신 릴리스 · 2026-09-22 · PT 움직임 3D 교정 v81 (배포 완료)

- 배포 커밋은 c606a90 Refine PT movement 3D coaching v81이며, GitHub Pages 실행 35697577115가 success로 완료됐다. 공개 사이트는 https://peoplenhpark.github.io/PT-GOLF/?v=81 이고 공개 3D·이미지 파일과 브라우저 오류 0개를 확인했다.
- 와이드 사이드 런지 스트레칭은 반대편 다리를 먼저 길게 펴고 발바닥을 고정한 뒤 지지 다리에 앉듯 진행한다. 3D는 전 재생 구간에서 반대 다리를 약 174도로 유지하고, 지지 다리만 약 89도까지 굽힌다.
- 고관절 오픈·클로즈 스트레칭은 지지 한발 힙힌지로 반대 다리를 뒤로 길게 뻗고 골반·가슴을 함께 여닫는 실제 수업 흐름으로 바꿨다. 준비·동작 2컷과 코칭도 일치시켰다.
- 백 익스텐션은 맨몸 힙힌지 시작 범위를 깊게 조정했다. 시작 머리 높이는 약 1.16m, 복귀 시에는 허리 과신전 없이 몸통 일자까지만 세운다.
- tests/pt-sidelunge-pose.cjs, tests/pt-hip-openclose-pose.cjs, tests/pt-backextension-pose.cjs가 각각 1,001프레임의 핵심 관절 기준을 검사한다. 기존 PT 프레스·골프 데이터 회귀, 변경 JS 문법, git diff --check도 통과했다.
- 다음 세션은 PT & GOLF VER1.4로 시작한다. 새 PT 동작은 준비·동작 2컷과 회전·재생 가능한 3D를 항상 함께 제공한다. 골프에는 이미지·3D를 추가하지 않는 현재 정책을 유지한다.


## 최신 로컬 작업 · 2026-09-22 · 백 익스텐션 깊은 힙힌지 v81 (미배포)

- 사용자 요청에 따라 백 익스텐션 3D의 앞으로 숙이는 시작 범위를 더 깊게 했다. 시작 머리 높이는 약 1.34m에서 1.16m로 낮아져 힙힌지 숙임이 분명해졌다.
- 올라오는 끝 자세는 기존처럼 엉덩이 힘으로 몸통 일자까지만 세우며, 허리를 뒤로 꺾지 않는다. 2컷·3D 시작 단계 캡션도 ‘힙힌지로 더 깊게 숙이기’로 갱신했다.
- tests/pt-backextension-pose.cjs를 추가해 깊은 시작 힙힌지, 중립 복귀, 맨몸 기구 구성과 1,001개 프레임의 유한값을 검사한다.
- 앱/SW/호환 주소는 81이다. CUA에서 변경된 2컷·회전·재생 3D·슬라이더와 브라우저 오류 0개를 확인했다. 이번 요청에는 배포 지시가 없어 커밋·푸시·배포는 하지 않았다.


## 최신 로컬 작업 · 2026-09-22 · 와이드 사이드 런지 반대 다리 선행 고정 v80 (미배포)

- 사용자 교정에 따라 와이드 사이드 런지의 순서를 ‘지지하지 않는 반대쪽 다리를 먼저 길게 펴고 발바닥을 고정 → 지지 다리 무릎을 굽혀 엉덩이를 뒤로’로 명시했다.
- 3D는 반대 다리가 재생 초반부터 끝까지 약 174도로 곧게 유지되고, 지지 다리만 약 89도까지 굽도록 발 위치 계산을 교체했다.
- 2컷 캡션·상세 코칭·3D 단계 제목을 같은 순서로 갱신했다. tests/pt-sidelunge-pose.cjs는 1,001개 전 프레임에서 반대 다리 각도 170도 초과를 검사한다.
- 앱/SW/호환 주소는 80이다. CUA에서 갱신된 2컷·3D 재생·회전·슬라이더와 브라우저 오류 0개를 확인했다. 이번 요청에는 배포 지시가 없어 커밋·푸시·배포는 하지 않았다.


## 최신 로컬 작업 · 2026-09-22 · 고관절 오픈·클로즈 실제 힙힌지 3D v79 (미배포)

- 첨부 세션의 실제 설명을 다시 확인해, 기존 무릎 들기 3D를 지지 한발 힙힌지로 교체했다. 지지 발·안쪽 허벅지로 바닥을 누르고, 반대 다리를 뒤로 길게 뻗은 뒤 골반·가슴을 함께 열고 정면으로 돌아온다.
- 안내 코칭과 준비·동작 2컷도 같은 형태로 갱신했다. 허리만 비트는 대신 고관절에서 접고, 지지 발을 고정한 채 범위를 조절하도록 안내한다.
- tests/pt-hip-openclose-pose.cjs를 추가해 1,001개 프레임의 유한값, 지지 발 고정, 지지 무릎의 가벼운 굽힘, 뒤로 뻗은 다리, 몸통 힙힌지·회전을 검사한다.
- 앱/SW/호환 주소는 79이다. CUA에서 새 2컷·회전·재생 3D·슬라이더와 브라우저 오류 0개를 확인했다. 이번 요청에는 배포 지시가 없어 커밋·푸시·배포는 하지 않았다.


## 최신 로컬 작업 · 2026-09-22 · 와이드 사이드 런지 3D 다리 정렬 교정 v78 (미배포)

- 사용자 피드백에 따라 와이드 사이드 런지 3D에서 런지 쪽 무릎만 굽히고 반대쪽 다리는 끝 범위에서 거의 곧게 펴지도록 바깥 발의 이동 궤적을 교정했다.
- 재생 중 최대 런지 자세에서 런지 쪽 무릎은 약 89도, 바깥쪽 무릎은 약 178도가 되며, 내전근이 늘어나는 형태를 유지한다.
- tests/pt-sidelunge-pose.cjs를 추가해 1,001개 재생 프레임의 유한값, 런지 쪽 무릎 굽힘과 바깥 다리 펴짐을 검사한다.
- 앱/SW/호환 주소는 78이다. 로컬 미리보기에서 준비·동작 2컷과 교정된 회전·재생 3D를 확인했다. 이번 요청에는 배포 지시가 없어 커밋·푸시·배포는 하지 않았다.


## 최신 로컬 작업 · 2026-09-22 · PT 신규·기존 동작 갱신 v77 (미배포)

- 첨부 세션 메모에서 운동 내용만 반영했다. 백 익스텐션은 덤벨 로우를 제외한 맨몸·통증 없는 범위의 힙힌지로 갱신하고, 준비·동작 2컷과 3D도 덤벨 없이 몸통을 중립까지 세우는 동작으로 바꿨다.
- 레그 프레스(와이드 스탠스)는 고관절 준비 후, 골반·허리를 패드에 고정하고 발바닥 전체로 밀며 무릎 정렬을 지키는 코칭으로 갱신했다.
- 신규 PT는 와이드 사이드 런지 스트레칭(내전근)과 고관절 오픈·클로즈 스트레칭이다. 두 동작 모두 준비·동작 2컷 WebP와 회전·재생 가능한 3D를 추가했고, 지지물·통증 없는 가동범위·좌우 수행을 안내한다.
- PT는 47개 시각 자료가 됐다. 앱/SW/호환 주소는 77이며, 로컬 미리보기는 http://127.0.0.1:8792/index.html?v=77#exercise/pt_hip_openclose_stretch 이다.
- 검증: golf-training-data, pt-press-media, 변경 JS 문법, 신규·갱신 3D 각 1,001프레임 유한값 검사, git diff --check, CUA에서 신규 2개 카드·2컷·3D 재생/일시정지/시점 전환/슬라이더 확인을 통과했다. visual-media는 설치된 런타임에 Playwright 패키지가 없어 실행하지 못했다.
- 이번 요청에는 배포 지시가 없어 커밋·푸시·배포는 하지 않았다.


## 최신 로컬 작업 · 2026-09-22 · PT 반응형 카드·세이지 라벤더 v76 (배포 승인)

- 사용자 요청에 따라 PT 부위별 목록을 병렬 카드로 변경했다. 520px 미만은 2열, 그 이상은 3열이며, 카드에는 번호·운동명·핵심 사양·즐겨찾기 상태만 표시한다. 준비·동작 이미지와 회전·재생 가능한 3D, 체크리스트와 메모는 기존 상세 화면에서 그대로 제공한다.
- PT 기본색은 세이지(`--pt #a7c7ac`, 배경 `#18291d`, 선 `#4a6e52`)로 낮추고, 완료·선택 상태만 라벤더(`--pt-secondary #b9add8`)로 구분했다. 골프의 청록·골드 계열은 변경하지 않았다. 라이트 테마에도 대응 색상을 추가했다.
- `js/app.js` 문법, `tests/pt-press-media.cjs`, `tests/golf-training-data.cjs`, `git diff --check`, CUA 390px 2열·넓은 화면 3열·가로 넘침 없음·브라우저 오류 0개를 통과했다. 설치된 런타임에 Playwright 패키지가 없어 `tests/visual-media.cjs`는 실행하지 못했으며, 실제 CUA 화면으로 확인했다. 앱/SW/호환 주소는 76이다.
- 사용자의 “배포해줘” 요청으로 v76 배포가 승인되었다. main push 후 GitHub Pages 공개 파일과 UI를 확인한다.
## 최신 로컬 작업 · 2026-09-20 · 골프 Shorts 5편 추가 v75 (미배포)

- 사용자가 공유한 YouTube Shorts 5편(aaOw2sdp-io, 5glZX2pq15o, U4nn7s20ACc, 4uQe-J5qM2c, 7sNhk9PhBxc)을 원본 영상 목록에 추가했다. 원본 제목·채널은 YouTube oEmbed, 길이(88/80/88/49/44초)는 공개 watch 페이지 메타데이터로 확인했다.
- 제목에 드러난 주제만 기준으로 백스윙·스윙 흐름 3편은 회전·체중이동, 훅·헤드 감각 2편은 팔·임팩트에 배치했다. 원본 영상의 세부 교정 내용을 추정하지 않고 비교 관찰 안내만 제공한다.
- 전체 23편·4개 그룹(2/3/9/9), 기본 영상 9YWDNMyTQy4와 병렬 카드·골드 구획색·골프 이미지·3D 제거 상태·PT 45개 3D 모델·개인 저장은 보존했다. 기본 영상이 목록에서 중복되지 않아 기본 목록의 팔·임팩트 섹션은 8개 카드로 표시된다.
- `tests/golf-training-data.cjs`, `js/golf-data.js` 문법, `git diff --check`, CUA 전체 23편·5개 ID·390px 가로 넘침 없음·브라우저 오류 0개를 통과했다. 앱/SW/호환 주소는 75이며 아직 커밋·푸시·배포하지 않았다.
## 최신 로컬 작업 · 2026-09-20 · 골프 영상 골드 구획색 v74 (배포 승인)

- 사용자 요청으로 추가 색상은 골드 `#C8A96B` 한 가지로 제한했다. 골프 유튜브의 그룹 제목과 편수에만 적용해 목록 구획을 빠르게 읽을 수 있게 했다.
- 선택된 그룹·원본 재생·기존 링크는 청록색을 유지하고, 카드 내용·영상 수·병렬 카드 배치·상세 설명·개인 메모는 변경하지 않았다.
- `tests/golf-training-data.cjs`, `js/golf.js` 문법, `git diff --check`, CUA 390px 2열·가로 넘침 없음·골드 색상 적용·브라우저 오류 0개를 통과했다. 앱/SW/호환 주소는 74이다.
- 사용자의 “적용해서 배포해줘” 요청으로 v74 배포가 승인되었다. main push 후 GitHub Pages 공개 파일과 UI를 확인한다.
## 최신 로컬 작업 · 2026-09-20 · 골프 영상 병렬 카드 v73 (배포 승인)

- 사용자 요청에 따라 골프 영상의 제목·채널·길이·원본 재생과 상세 설명·개인 메모를 그대로 두고, 그룹 목록 카드의 배치만 병렬 격자로 변경했다.
- 모바일은 2열, 720px 이상은 3열이다. 기본 영상은 기존 상단 단일 강조 카드를 유지하며, 각 카드의 제목 링크에서 상세 설명과 메모를 계속 확인할 수 있다.
- 전체 18편·4개 그룹, 골프 이미지·3D 제거 상태, PT 45개 3D 모델, seed·개인 저장 동작은 변경하지 않았다.
- `tests/golf-training-data.cjs`, `js/golf.js` 문법, `git diff --check`, CUA 390px 2열·768px 3열·가로 넘침 없음 및 브라우저 오류 확인을 통과했다. 앱/SW/호환 주소는 73이다.
- 사용자의 “내용은 살리고 배치만 병렬로 구현해주고 배포까지 해줘” 요청으로 v73 배포가 승인되었다. main push 후 GitHub Pages 공개 파일과 UI를 확인한다.
## 최신 로컬 작업 · 2026-09-20 · 골프 영상 카드 밀도 v72 (배포 승인)

- 사용자 요청으로 v71의 핵심 이미지 3장·원본 프레임 설명·관련 캐시 자산을 모두 제거해 상세 화면을 이전 구성으로 되돌렸다. 골프 이미지·3D 제거 상태와 개인 기록은 유지한다.
- 전체18편의 그룹 카드가 동일한 작은 사각형 카드가 되도록 통일했다. 카드에는 제목·채널·길이·원본 재생만 표시하고, 상세 설명과 메모는 제목 링크에서 확인한다. 이후 영상이 늘어나도 목록 밀도를 유지한다.
- 320px와390px 화면에서 기본 영상과 프로 스윙2편, 총3개의 카드가 한 화면에 완전히 표시된다. 그룹 선택은 가로 스크롤로 유지하고 가로 넘침은 없다.
- `tests/golf-training-data.cjs`, 변경JS 문법, `git diff --check`, CUA 320/390px 확인과 브라우저 오류0개를 통과. 앱/SW/호환 주소는72.
- 사용자의 후속 “배포해줘” 요청으로 v72 배포를 승인받았다. 정상 main push 후 GitHub Pages 성공과 공개 파일/UI를 확인한다.
## 최신 로컬 작업 · 2026-09-19 · 기본·프로 스윙 작은 카드 v69 (배포 승인)

- 사용자 요청으로 `유튜브 영상 기본 목록 설정` 작업(01a0b8e7-4ebe-7082-b3ca-bf8d78c1b6ea)의 메시지와 로컬 v68 변경을 이 `PT & GOLF VER1.3` 작업에서 이어받았다. 원래 작업의 대화 자체를 병합하거나 이동한 것은 아니다.
- 기본 영상과 프로 스윙 시범의 카드만 제목·채널·길이·원본 재생 버튼으로 축소. 제목 링크는 기존 설명/개인 메모 상세로 연결하고, 원본 버튼은 YouTube 새 탭을 연다. 적용 상태도 있으면 표시한다. 다른 그룹과 상세 화면의 설명·주제는 유지.
- 390px 기준 기존 카드 높이: 기본317.6px→76px(24%), 프로367.3/391.8px→각84.5px(23%/22%). 고정 높이/텍스트 잘림 없이 작은 화면에서는 제목이 자연스럽게 줄바꿈된다. 링크 터치 영역은 52px 이상.
- Shorts5편을 포함한 전체18편, 기본9YWDNMyTQy4 상단 고정/중복 제외, 네 그룹 및 김민지5 첫 순서를 보존. PT45개 시각 자료, 골프 이미지·3D 제거, seed/개인 저장 기록 불변.
- 검증: 기존 tests/golf-training-data.cjs, js/golf.js 문법, git diff --check 통과. CUA 320/390/768px 카드/가로넘침 검사, 18개 중 작은 카드3개, 프로 그룹2개, 상세 설명·메모 및 목록 복귀 정상, 브라우저 오류0개. 개인 기록 저장 없음.
- 앱/SW/공통 자산69. 미리보기: http://127.0.0.1:8792/index.html?v=69#golf/videos . 사용자의 후속 “배포해줘” 요청으로 v68 영상 추가와 v69 카드 변경의 배포를 승인받음. 배포 전 골프 회귀·변경 JS 문법·diff 검사 통과 및 origin/main과 동기화 확인. 정상 main push 후 GitHub Pages 성공과 공개 파일/UI를 검증한다. 직전 라이브는 v67(5779f8b).

## 최신 로컬 작업 · 2026-09-19 · 골프 Shorts 5편 및 기본 영상 v68 (미배포)

- 사용자 제공 Shorts 5편 추가: ojzyFHAQWnw, wBlnDaqkGi0, yv4KTSs8Riw, 4ePhtleqZBA, 9YWDNMyTQy4. 원본 제목·채널은 YouTube oEmbed, 길이(12/11/43/16/18초)는 공개 watch 페이지 메타데이터로 확인했다. 영상 내용 분석 요청은 없어 상세 교정 요약을 추정하지 않고 비교 관찰 안내로 표시했다.
- 마지막 9YWDNMyTQy4(정회엽프로, 7번 160m 아래로 내려놓기)를 골프 유튜브 첫 화면의 기본 영상으로 지정. 그룹 선택·검색 위에 강조 카드로 표시하며 목록에서 중복하지 않는다. 그룹/검색에서는 해당 조건을 따른다. 기존 김민지5 영상은 프로 스윙 시범 그룹의 첫 순서를 유지한다.
- 전체18편/4그룹(2/3/6/7). 기존13개 원본, 골프 이미지·3D 제거, PT45개 시각 자료, seed·개인 기록 보존.
- 앱/SW/자산 버전68. tests/golf-training-data.cjs, 변경JS문법, git diff --check 통과. 기본영상 순서·중복제외·검색·그룹필터 회귀 검사 포함.
- CUA 검증: 390px 첫 화면에서 기본 영상 카드 전체가 그룹보다 위에 보임(상단196px/하단548px), 영상카드18개, 원본/상세 링크 정상. 320px 상세 메모 유지·가로넘침 없음. 기존 서버 종료로 인한 SW 오류는 서버8792 재시작 후 최신화하여 해결.
- 로컬 미리보기: http://127.0.0.1:8792/index.html?v=68#golf/videos . 커밋·푸시·배포 전이며 라이브는 v67 (5779f8b).


## 최신 릴리스 작업 · 2026-09-18 · PT 프레스 2종 시각 자료 v67 (배포 승인)

- 사용자 상시 지침: 앞으로 신규PT는 준비·동작2컷과 회전·재생 가능한3D를 항상 함께 추가한다. `AGENTS.md` 및 `.cursor/rules/pt-media.mdc`에 영구 기록했다.
- 사용자 요청으로 덤벨 프레스(pt_dumbbell_press)와 인클라인 스미스 벤치프레스(pt_incline_smith_press)에 준비·동작2컷과 회전/재생형3D를 추가했다. 두 운동 모두 focus → 2컷 → 입체로 자세 보기 → 기존 코칭/메모 순서를 따른다.
- 내장 image_gen으로 은색 인체·붉은 가슴·차콜 배경의2컷을 생성하고4개WebP로 연결했다. 최종 경로와 프롬프트: `docs/visuals/pt-press-image-prompts.json`. 원본PNG는 ignored `docs/images/guides/source/`에 보존.
- `dumbbellpress`: 수평 벤치·각각의 덤벨·천천히 받아 살짝 모아 미는 궤적. `smithincline`: 경사 벤치·수직레일·바와 슬라이딩 칼라가 같이 움직이는 모델. 손목과 팔꿈치의 하단 수직 정렬, 발/골반/몸통 고정, 고정 관절 길이를 유지한다.
- 시각 자료는 예시이며 실제 수업의 각도·중량 재현은 아니다. 이 안내를2컷과3D에 표시했다. seed34/49운동/원문/날짜/개인 기록은 그대로다. 골프 이미지·3D 제거도 유지된다. PT45개 모두2컷+3D를 갖는다.
- 앱/SW/공통뷰어와 호환 주소를 v67로 맞췄다. 라이브는 직전 v66 커밋8d2834d77d625ecd55ece644c3456689f643e772, Pages35311294425 success이며 공개15파일·실제UI 검증 완료 상태다. 사용자가 후속 “배포해줘”로 v67 배포를 승인했다. main 정상 push 후 해당 Pages 실행 성공과 공개 파일/UI를 확인한다.
- 검증: `tests/pt-press-media.cjs` 2,002포즈의 관절길이/연속성/지지고정/덤벨분리/스미스레일/이미지형식/코칭일치 통과. 기존PT882포즈 및43개registry가 이전커밋과 동일, seed파일 불변. `tests/golf-training-data.cjs`의13원본/4그룹/골프3D제거/과거주소7경우 통과.
- CUA에서 실제3D준비·밀기자세, 시점전환, 느린재생/일시정지/슬라이더,2컷4장로딩,2운동×320/390/768px 가로넘침없음·iframe잘림없음·접을때제거·메모영역유지 확인. JS오류없음. 사용자기록에 테스트값을 저장하지 않았다.
- 미리보기: http://127.0.0.1:8792/index.html?v=67#exercise/pt_dumbbell_press 및 pt_incline_smith_press. 이 작업의 로컬서버는 기존 세션87868/8792를 재사용했다.


## 최신 릴리스 작업 · 2026-09-18 · v66 / seed34 (배포 승인)

- 배포: 2026-09-18 사용자 승인. 배포 전 골프 회귀·변경 JS 문법·diff 검사 통과, 원격 main과 동기화 확인. main 정상 push 후 해당 커밋의 Pages 성공 및 라이브 파일/UI를 확인한다. 아래 v65 배포 기록은 이전 릴리스 이력이다.
- 사용자 요청: 골프 전체의 3D 이미지 삭제, `새로운 노트 (18).txt`의 PT 추가·업데이트.
- 골프 유튜브 13편·네 그룹과 원본/편집 설명/개인 기록은 유지. 전역 카드·영상 상세·관련 자료에서 3D 옵션·안내를 제거했다.
- 골프 스윙 노트 4종은 정지 2컷과 회전형 3D 모두 화면에서 제거했다. 이 범위는 사용자에게 질문 후 응답이 없어 명시한 가정이다. 개인 overlay의 이미지도 화면에 노출하지 않으며 저장 데이터는 수정하지 않는다.
- 예전 golf3d viewer/lesson/consistency/training/original HTML 및 공통 PT viewer의 골프 주소는 원본 설명/해당 노트로 이동. 3D 모델·텍스처는 선캐시에서 제외했다. 소스·라이선스·과거 자산은 복구 자료로 남아 있으나 앱에서 로딩하지 않는다.
- PT 기존 43개 운동의 2컷/3D 유지. 풀오버·푸시다운 코칭과 캡션 보강, 신규 인클라인 스미스 벤치프레스/덤벨 프레스는 가슴 카테고리에 코칭으로 추가했다. 신규 두 운동은 아직 이미지·3D가 없다.
- 총49개(PT45+골프4), seed34, 앱/SW 자산66. 이전 라이브는 v65 / 7f633495854995b7b8d9c7bba4120534d7194f72이며 사용자가 후속 “배포해줘” 요청으로 이번 변경 전체의 배포를 승인했다.
- 녹취 근거와 수치 불확실성: `docs/PT_2026_09_18_UPDATE.md`. 푸시다운 상체 살짝 숙임은 확인됐지만 중량·손잡이·정확한 각도·세트 수는 여전히 미확인이다.
- 검증: `tests/golf-training-data.cjs`로13개 원본/4그룹/렌더링/캐시/과거 주소7경우/저장 호출0회 통과. 기존45개 운동 및 원칙 불변·메모/즐겨찾기 기본값 보존 확인. CUA390px에서 골프4종 이미지·3D0개, 기존 PT2종은2컷+3D 유지, 신규2종 코칭·9/18표시·가로넘침없음 확인. 풀오버3D 실제 로딩 확인. 개인 기록에 테스트 값을 저장하지 않음.
- `tests/visual-media.cjs`의 기대값은 골프/신규 PT의 이미지 없는 상태에 맞췄지만 전체 WebGL 재검사는 이번에 실행하지 않았다. 과거 golf-3d/golf-lesson/golf-player 등은 은퇴한 3D 구현의 이력용 검사다. 현재 골프 회귀 검사는 golf-training-data가 기준.


> 최종 갱신: **2026-09-16 · PT & GOLF VER1.3 세션 핸드오프**
> 라이브·코드 **v65 / 7f633495854995b7b8d9c7bba4120534d7194f72** · seed **v33** · SW **ptgolf-v65**
> 배포 성공 확인 완료. 새 세션 시작 문서: `docs/PT_GOLF_VER1_3_HANDOFF.md`. 아래의 미배포 표시는 과거 작업 당시의 기록이며 현재 상태가 아니다.
> 이 문서는 이전 대화 없이 다음 에이전트가 작업을 이어갈 수 있도록 현재 상태, 결정, 코드 구조, 검증 및 이월 사항을 함께 담는다. 과거 기록과 충돌하면 본문의 현재 기준과 사용자의 새 요청을 우선한다.
> 이 문서 자체의 최종 커밋은 `git log -1 -- docs/AGENT_HANDOFF.md`로 확인한다. v51의 전체47개 운동 배치 위에 골프 학습 허브를 추가했다. 최신 커밋은 git log로 확인한다.

## 새 세션 준비 · PT & GOLF VER1.3

- 2026-09-16 사용자 요청으로 새 세션용 핸드오프를 작성했다. VER1.3은 새 작업 이름이며 앱 자산 버전65와 별개다. 이름만으로 앱 버전·seed·브랜치를 변경하지 않는다.
- v65 배포 성공: GitHub Pages 실행34941539155, 커밋7f633495854995b7b8d9c7bba4120534d7194f72. 라이브11파일 일치, 유튜브 기본 진입, 전체13편/네그룹/원본13개, 팔·임팩트4편 필터와 상세 복귀, 새 영상의 실사형 모델 로딩과 원본 연결 확인 완료.
- 인수인계 시작 시 코드 작업 트리는 깨끗했다. 이번 요청은 핸드오프 문서 작성과 새 작업 준비이며, 새로운 기능 구현이나 추가 배포 요청은 없다. 이번 문서 변경은 로컬에 보관한다.
- 자세한 현재 기준은 새 문서를 먼저 읽고, 이 문서는 과거 경과·기술 구현 참고용으로 사용한다.

## 현재 릴리스 v65 — 영상 13편, 유튜브 기본 진입, 주제별 그룹 배포

- 사용자가 “베포해줘”로 v64~v65 변경 전체 배포를 승인했다. 아래 미배포 표시는 승인 전 작업 기록이다.
- 새 원본 영상3편으로 총13편, 유튜브 → 레슨 → 스윙 노트 순서 및 유튜브 기본 진입, 네 하위 그룹(2/2/5/4편), 그룹별 주소와 상세 복귀 연결을 포함한다.
- 배포 전 영상별 길이·원본 정책·관련 링크·3D 링크·캐시·그룹 분류의 중복/누락 검사 및 JS 문법, diff 검사를 통과했다. 기존 개인 기록과 seed를 변경하지 않았다.
- 대표 주소 https://peoplenhpark.github.io/PT-GOLF/?v=65#golf/videos . 게시 후 기본 진입/그룹별 화면/새 영상 원본과3D 연결을 확인한다.

## 이전 작업 v65 — 유튜브 하위 그룹 (로컬, 미배포)

- 사용자 요청: 영상이 많아져 적절한 서브 그룹으로 정리. 프로 스윙 시범2편 / 준비·자세2편 / 회전·체중이동5편 / 팔·임팩트4편으로 분류했다. 각 영상은 주된 그룹 한 곳에만 속하며 기존 주제 태그·관련 영상으로 여러 주제를 연결한다.
- GolfContent.videoGroups / videoGroupFor로 분류를 정의. 전체 보기에서는 네 그룹별 제목·설명과 영상을 표시하고, 상단 그룹 버튼 또는 ‘이 그룹만 보기’로 필터링. 그룹 내부 순서는 원본 videos 배열 순서이므로 첫 프로 영상2편 우선순위 유지. 미분류 데이터가 추가되면 기타 영상에 표시하여 목록에서 사라지지 않게 함.
- #golf/group/pro-swings, setup, rotation, arms-impact 주소로 직접 진입/새로고침 가능. 상세 상단은 해당 그룹 복귀 및 전체 보기 링크. 유튜브 기본 진입/탭 순서/원본13·3D12/에이밍 제외 정책 유지. 기존 골프 전체 검색은 그대로 유지.
- 검증:13편 중복·누락 없음, 그룹 수4와 분류2/2/5/4 검사 추가. 기존 영상정책/캐시/문법/diff 검사 통과. CUA로 네 그룹별 개수/단일 섹션, 그룹 주소 새로고침, 상세→그룹 복귀, 전체13개/첫김민지영상, 390px 가로넘침 없음 확인.
- 로컬 자산/SW v65. v64에서 추가한3편 및 기본유튜브 전환도 포함하나 아직 배포 전이며 라이브는 v63/a1b35f3. 개인 기록·seed 변경 없음.
- 미리보기 http://127.0.0.1:8792/index.html?v=65#golf/videos .

## 이전 작업 v64 — 영상 3편 추가와 유튜브 기본 진입 (로컬, 미배포)

- 사용자가 Aj1UEMYPxBg, xUgGGs2Rh3w, S3fxUFBzfBo 3편 추가를 요청한 뒤 탭 순서를 유튜브 → 레슨 → 스윙 노트로 변경하고 유튜브를 기본값으로 지정했다. 두 요청 모두 반영.
- 신규 원본 메타데이터: 남프로TV & 브릿지골프 오른팔 가속 53초, 조윤성프로 일관성/오른쪽 어깨 104초, 이석영프로 팔과 몸 회전 90초. 공개 원본 제목/길이, 재생 화면의 자막/시범, 세 번째 영상 설명란을 확인했다. 자막 파일 조회는 빈 응답이었으므로 전체 자막 수집으로 기록하지 않는다.
- 기존 최상단 김민지5 및 다음 드라이버 영상은 유지하고 그 뒤에 요청 순서대로 3편 삽입. 총13편, 원본13개/3D12개(에이밍 제외). 새 영상은 원본+핵심3항목+확인한 구간 링크+공통7번아이언 모델을 제공한다. 원본별 맞춤 동작 복원은 아님. 관련 기존 영상에도 새 영상으로 돌아오는 링크 추가.
- 골프 내부 탭/홈 안내 순서 변경. 홈 골프 카드, 하단 골프 버튼, 기본 golf-hub 경로와 #golf/#golf/ 모두 유튜브로 진입. 명시적인 #golf/notes 및 #golf/lessons, 기존 노트 상세 링크는 유지.
- index/app/SW 및 관련 페이지의 버전은64. 영상 데이터13편/길이/중복/관련 링크/3분 기준/캐시 검사, 문법, diff 검사 통과. CUA로 홈/하단/#golf 진입, 레슨/노트 전환, 새3편 상세의 3개 요점과 원본/3D 링크, 390px 가로 넘침 없음 확인. 새 영상→실사형 모델→해당 원본 href 확인, 페이지 오류 없음.
- 사용자 메모·seed 변경 없음. 이번 요청에 배포 지시가 없으므로 로컬 파일에만 반영. 대표 미리보기 http://127.0.0.1:8792/index.html?v=64#golf/videos .
- 직전 v63 배포는 GitHub Pages 실행34928075732 success, commit a1b35f383e5e5b4511198ae637c1d6d4479cee80. 라이브21파일 일치 및 원본10/3D9/실사형 모델 로딩 확인을 완료했다.

## 이전 릴리스 v63 — 실사형 골퍼와 원본 영상 선택 배포

- 사용자가 2026-09-15 “배포해줘”로 v61~v63 로컬 변경 전체의 GitHub Pages 배포를 승인했다. 아래의 미배포/미커밋 표시는 이 승인 이전 기록이다.
- 실사형 Rocketbox 골퍼(라이선스 포함), 10편 전체 원본 영상 보기, 9편 3D 옵션과 에이밍 원본 유지, 3D 화면에서 관련 원본으로 이동하는 기능을 포함한다.
- 배포 전 영상 정책/캐시, 모의 YouTube API 예외 처리, 4클럽×1,001포즈(4,004개) 모델 검증 통과. 로컬 모바일 검증 결과는 아래 v63 작업 기록 참조. seed/개인 데이터 변경 없음.
- 대표 주소: https://peoplenhpark.github.io/PT-GOLF/?v=63#golf/videos . 게시 후 원격 파일과 실제 UI를 확인한다.

## 이전 작업 v63 — 실사형 3D와 원본 영상 선택 (배포 전 기록)

### 세션 마무리 확인 · 2026-09-15

- 사용자 요청에 따라 전체 적용 여부를 확인하고 세션 종료. 유튜브 10편 모두 길이와 관계없이 원본 전체 보기 제공; 9편은 실사형 3D 선택, 에이밍 1편은 이전 삭제 요청대로 원본만 제공한다.
- 네 클럽이 사용하는 공통 viewer 및 두 전용 레슨에 원본 이동 옵션이 포함됨. 180초 기준은 편집 표현 분류이며 원본 접근을 제한하지 않는다.
- 최종 전체 연결 검사, 기존 영상 분류/캐시 검사, git diff --check 통과. 사용자 기록과 영상 순서 유지.
- 현재 파일은 저장되어 있으나 v61~v63 변경은 미커밋·미배포. 라이브는 v60. 세션 종료 요청을 배포 승인으로 해석하지 않았음. 이후 배포 요청 시 이 변경 전체를 검토하여 기존 GitHub Pages 배포 절차를 진행할 것.


- 사용자 요청으로 유튜브 목록/상세에 두 보기 옵션을 나란히 추가. 3D 전용 레슨 2편은 해당 레슨, 나머지는 공통 아이언/드라이버 모델로 연결하고 설명에 공통 예시임을 표시한다. 에이밍은 3D 제공 안 함으로 유지. 원본은 모든 10편에서 YouTube watch를 직접 열어 이전 인앱 임베드 문제를 피한다. 기존 앱 안 재생 버튼은 보조 기능으로 유지.
- GolfContent.modelOptionFor가 연결을 결정하며 기존 180초 원본 기준은 변경하지 않음. 3D는 원본 선수의 외형/동작 복원이 아님을 명시. v62 샘플은 파일을 보존하고 기본 진입 링크는 두 보기 옵션으로 대체.
- viewer/lesson/consistency에 view-options.js를 추가하여 상단에 현재 3D와 원본 링크/영상 설명 복귀를 표시. 일반 viewer는 source ID를 따라 돌아가고, 두 전용 레슨은 각각 해당 원본에 고정한다. 단독 뷰어의 원본은 관련 참고 영상이며 CMU 동작 출처는 하단에 별도 유지.
- v63 캐시와 자산 참조 통일; app ASSET_VER도63. 개인 기록/seed/영상10편 순서 보존.
- 검증: 문법·기존 영상 분류/캐시·diff 검사 통과. CUA 390px에서 목록10개 선택 쌍, 상세 → 실사형 골퍼 → 해당 원본 href 확인; 3D 두 레슨 정상 로딩, 올바른 원본 href, 가로 넘침/페이지 오류 없음. 원본 직접 재생은 이전 턴에서 확인됨.
- 미리보기 http://127.0.0.1:8792/index.html?v=63#golf/videos . 아직 배포하지 않았으며 라이브 v60 유지.

## 이전 작업 v62 — 원본 선수 샘플 (로컬, 미배포)

- 사용자 요청: 유튜브 한 편을 원본 속 선수로 표현하는 샘플. 첫 번째 김민지5 프로 bfMsJtV61hM을 선택. original.html/css/js에서 원본 YouTube 플레이어와 정면/측면 관찰 설명(각 3개, 16.5px 굵게)을 함께 표시한다. 이는 영상 재생 샘플이며 선수 3D 복원이나 모션캡처 결과가 아니다. 기존 v61 실사형 3D는 보존했다.
- 원본 watch UI에서 0/5/10/15/16/17.327/18.46/20/25초 실제 프레임 확인. 정면 이후 17초에 측면 준비 자세. 4번 아이언, 31초, 원본 채널 골족남TV. 자막 없는 시범이므로 설명은 편집자 관찰 포인트로 표시.
- 유튜브 첫 영상 상세에 sampleHref/sampleLabel로 연결, 영상 순서/3분 이하 기준/기록 저장 방식 유지. 샘플 → 원본 메모/3D 원리/아이언 노트 연결. v62 자산과 SW 캐시 적용.
- 현재 인앱 브라우저에서는 원본 watch 재생은 정상이나 교차 출처 iframe이 빈 화면으로 남아 API ready가 도착하지 않는다. nocookie/공식 기본 호스트 모두 동일. 직접 embed 탐색은 referrer 없는 상태의 오류153을 보임(임베드 실패 원인 단정 불가). 20초 타임아웃 후 빈 플레이어를 숨기고 해당 시점 원본 링크를 표시. 실제 임베드 재생/동기화/반복은 이 환경에서 검증되지 않았다.
- 검증: tests/golf-original.cjs의 모의 API로 재생 전 시점 선택, 17초 설명 전환, 구간 반복/종료/일시정지, 오류 후 링크 전환 통과. 기존 영상 분류/캐시 테스트 및 diff 검사 통과. CUA에서 상세 화면 → 샘플 링크와 390px 가로 넘침 없음/시점별 3개 설명/16.5px 700 확인. SW는 외부 요청을 가로채지 않고 동일 출처 앱 자산만 처리하도록 보완. 실제 임베드 재생 검증은 미완료.
- 로컬 미리보기: http://127.0.0.1:8792/media/golf3d/original.html?v=62 . 실제 배포는 계속 v60. v61 모델 변경까지 전체 미커밋 상태이므로 보존할 것.

## 이전 작업 v61 — 실사형 3D 골퍼 (로컬, 미배포)

- 사용자가 실사형3D골퍼를 선택해 기존 기하학 인형을 Microsoft Rocketbox Male_Adult_01의 텍스처·스키닝 골퍼로 교체했다. 폴로셔츠·반바지·얼굴·피부·손가락을 갖춘 인물이며 특정선수 재현은 아니다. MIT 라이선스 포함.
- player.js + player/모델·텍스처파일(라이선스/출처포함), tools/build_golf_player.mjs, tests/golf-player.cjs 추가. 기존THREE를 재사용하며 웹용실데이터약2MB. CMU기존동작을80개뼈에연결,그립에손목을맞추고손가락굽힘적용. 기존poses.js/motion.js변경없음.
- viewer.js에서 인물 준비 후 기존단순몸메시만 숨기고 클럽/공/타깃/강조/카메라를 재사용. 준비중표시 및로드실패안내. 네클럽,손목레슨전신,일관성레슨전체에 적용. 어깨표시는 실사형을가리지않게축소.
- 자산/SW v61로통일,신규인물코드·메시·텍스처선캐시. seed·개인메모·유튜브10편순서 유지. 실제배포는여전히v60,이번턴별도배포요청없음.
- 수치검증:4클럽×1,001포즈=4,004,80뼈의유한행렬/양수배율/연속성,샘플스킨정점유효성 통과. 최대관절이동0.017474m,회전0.078611rad,손목–그립거리0.061561m. 브라우저390px 4클럽준비/임팩트/피니시에서realistic표시·가로넘침없음,두레슨로드와기존설명확인.
- 재현도구: C:/Users/peopl/Documents/PT-GOLF-samples/realistic-golfer/three-runtime/package (공식Three.js0.160.1 npm아카이브),원본FBX/TGA는그상위디렉터리. 변환된배포파일만repo에둠. 원본FBXSHA256는player/SOURCE.json.
- 미리보기 http://127.0.0.1:8792/media/golf3d/viewer.html?exercise=golf_iron7&v=61 .

## 이전 배포 릴리스 v60 — 일관성 레슨을 골퍼 모델로 교체

- 사용자가 추상 어깨·클럽 모형이 와 닿지 않는다고 요청하여 consistency.html/js의 별도 모형·별도 캔버스를 제거했다. 모든7장면에서 기존 CMU64_01 골퍼를 사용하며 전신/상체 확대/임팩트 확대3시점을 제공한다.
- viewer.js는 consistency-page에서만 어깨색(왼쪽파랑/오른쪽주황),몸통축,클럽헤드강조 및 실제 모델 헤드궤적을 켠다. setLessonFocus로 시점별 고정 프레임을 선택한다. 기존 viewer와 손목 lesson에는 강조가 적용되지 않는다. poses.js/motion.js와 관절 궤적은 변경하지 않았다.
- 임팩트는 정규화스윙 .708~.742, 얕은통과 .715~.742의 공 주변으로 좁혀 골퍼·손·헤드가 함께 크게 보인다. 프레임 범위는 시작/끝을 포함한 샘플로 고정, 수동 임팩트 시점에서 이 구간 밖이면 전체스윙 범위를 쓴다. 축·궤적은 설명용이고 영상인물 모션캡처가 아님을 유지.
- 60초7단계,3개설명,16.5px700이상,원본시점 및 관련 영상 연결 유지. 새쇼츠2편은 계속 목록1/2번,총10편. SW ptgolf-v60, 변경data/consistency/viewer.js는v60. 나머지자산v58유지. 사용자데이터/seed변경없음.
- 검증:320/390/768px×7단계에서 단일canvas·CMU골퍼·시점전환·설명3개·활성1개·가로넘침없음 확인. 실제재생·일시정지·끝정지·다시재생·수동확대와큰글씨확인. 기존손목lesson은2canvas정상·강조off. 새페이지오류로그없음. 문법·영상정책/캐시검증 및 diff공백검사통과.
- 2026-09-15 사용자 “배포해줘” 승인. v55~v60의 로컬 변경 전체를 기존 main/GitHub Pages에 게시하는 릴리스. 자산 참조를v60으로 통일했으며 seed/개인 데이터는 변경하지 않았다.
- 배포 URL https://peoplenhpark.github.io/PT-GOLF/?v=60#golf/videos 및 https://peoplenhpark.github.io/PT-GOLF/media/golf3d/consistency.html?v=60 . 실제 배포 상태는 해당 커밋의 Pages 실행에서 확인한다.

## 이전 작업 v59 — 쇼츠 2편 최상단 추가 (로컬, 미배포)

- 사용자 요청 bfMsJtV61hM을 유튜브 목록 첫 번째에, 이어 받은 -h77kU-fpjg를 두 번째에 배치했다. 기존8편 순서는 그대로여서 총10편.
- 공개 메타데이터 기준 각각31초/22초로3분 이하 원본 유지. 원본 제목·설명·화면을 확인했고 자막이 없는 시범 영상이므로 편집 관찰 포인트임을 명시. 실제 재생기는 초 미만을 버려30/21초로 보일 수 있음.
- bfMsJtV61hM: 골족남TV의 김민지5 프로4번 아이언 정면·좌측면 시범. -h77kU-fpjg: golfbox의 헤드 던지는 감각 드라이버 시범(인물은 업로더 설명에 근거해 표기).
- 기존 원본 플레이어·노트·레슨질문 흐름 재사용. 관련 영상과 상호 연결. golf-data.js만v59, SW ptgolf-v59로 갱신; 나머지3D자산v58유지. 개인 저장 데이터/seed변경없음.
- 검증: 데이터 문법·기존 분류/시점/캐시 검증, 브라우저 목록10편과 맨 위2개 순서 및 원본 링크 확인.
- 미리보기 http://127.0.0.1:8792/index.html?v=59#golf/videos . 미배포.

## 이전 작업 v58 — 영상 2편 추가 (로컬 완료, 미배포)

- IsSS-GnQQyY: 조윤성프로 「일관성 있는 스윙 만드는 방법」 공개 메타데이터611초(10:11), 원본 자막 확인. consistency.html/js에60초7단계3D 재구성. 기울어진 축 주위 어깨 회전 모형, 임팩트 페이스·얕은 지면 통과 모형, 기존 CMU 전신 흐름3시점. 모형은 교육용이며 영상 속 인물의 모션캡처나 각도 측정이 아니다.
- ULOLFCC-ly8: 깡프로 힘·타이밍92초(1:32), 원본 자동자막 확인.3분 이하이므로 원본 플레이어+5개핵심설명+5개시점링크. 0/5는 주관적 감각, 놓기는 과한 긴장 완화이며 클럽을 놓는 지시가 아님을 명시.
- 유튜브8편. 두 영상 상호 연결, 기존 손목·체중이동에서 새 영상으로도 연결. golf.js의3D버튼은 영상별lessonHref/lessonLabel지원, 기존 임팩트는 기존주소 유지. 스윙 노트·레슨질문·개인메모 흐름 유지.
- 3D설명은 단계별3항목·16.5px700, 상세21px700과 제목28.5px800 유지. 좁은 화면에서는 카메라 거리를 종횡비에 맞춰 조절. 축/페이스 모형은 드래그·방향키 회전 지원.
- 자산v58/SW ptgolf-v58,새2파일선캐시. seed33·개인 저장 데이터 변경 없음. 에이밍3D 제거 상태 유지. 라이브는v54이며 이번 배포 요청 없음.
- 검증: node --check, tests/golf-training-data.cjs(3분 경계·새 원본초/분류/상호링크/전체시점/캐시) 및 git diff --check 통과. CUA 실제320/390/768px에서7단계 각각3설명·활성1개·canvas준비·가로넘침없음. 60초끝정지·다시재생·일시정지·탐색,긴영상 전용3D링크,짧은영상nocookie임베드,목록8편 확인. 새페이지콘솔오류없음.
- 로컬 http://127.0.0.1:8792/media/golf3d/consistency.html?v=58 및 http://127.0.0.1:8792/index.html?v=58#golf/videos . 기존8792서버 사용.

## 이전 작업 v57 — 원본 유지 3분 이하, 에이밍 3D 제거 (로컬)

- 사용자 요청에 따라 원본 유지 기준을 180초 이하로 변경했다. 정확히3분도 포함. GolfContent.originalMaxSeconds와 presentationFor에서 분류하며 명시적 원본 선택이 우선한다.
- 에이밍·셋업 3D와 연결 버튼을 제거했다. 원본 영상·편집 설명·기존 사용자 메모는 유지한다. 체중이동2:46도 원본+편집 설명으로 전환. 손목·임팩트3D는 유지.
- training.js/css/data.js 제거, viewer.js의 체중이동 전용 강조 렌더링과 CSS 제거. training.html은 이전 aiming/shift 링크를 해당 원본 자료로 보내는 호환 페이지로 축소.
- 삭제한 로컬3D 파일은 C:/Users/peopl/Documents/PT-GOLF-samples/retired-training-v56 에 복구용 보관. 서비스 자산/캐시에서는 제거했다.
- 자산v57·SW ptgolf-v57. seed33 및 사용자 데이터는 변경하지 않았다. 아직 미배포.
- tests/golf-training-data.cjs: 3분 경계 포함, 3:01 분류, 에이밍 예외, 체중이동 원본, 임팩트3D 유지, 연결/캐시 자산 검사 통과. CUA에서 이전 에이밍 주소의 원본 이동, 체중이동 원본+편집 설명/3D버튼0개 확인.
- 미리보기 http://127.0.0.1:8792/?v=57#golf/videos/CA-TZ7WQlHY . 서버81467/8792 유지.

## 이전 작업 v56 — 길이 기준 영상 2편 추가 (로컬 구현)

- 사용자 기준: 원본2분 초과→3D 핵심편집,2분 이하→원본과 편집 설명. 새 원본 UA-HYcmiKTA는15:53, CA-TZ7WQlHY는2:46으로 둘 다3D 대상임을 확인하고 사용자에게 안내.
- 에이밍·셋업80초8단계: 코스/구질, 공 뒤에서 보기, 페이스 먼저, T자, 평행 레일, 가까운 기준, 탄도셋업, 안전한 공략. 독립 Three.js 배치도. 체중이동60초6단계: 긴장완화,시프트,왼발지지,골반회전,타이밍,전체연결. 기존 CMU 전신과 교육용 고리/화살표 결합.
- training.html?lesson=aiming 또는 shift, training.js/css/data.js 추가. 각3D↔자료/원본시점/관련노트/다른3D 연결. 유튜브는6편. 손목·임팩트 기존 레슨도 상단에서 이어보기.
- 기존150%굵은 글자와 각 단계3개설명을 유지. 원본 속 인물의 모션캡처나 실제 압력측정으로 주장하지 않음. SOURCE.md에 출처와한계 기록.
- 전 자산v56,SW ptgolf-v56,새4파일선캐시. seed33 및 개인데이터 미변경. 현재 미배포,라이브v54. v55/v55.1 작업과 함께 아직커밋되지 않은 로컬변경.
- 검증: tests/golf-training-data.cjs 원본초/편집초/단계/출처/6개중복없는영상/캐시파일/1,001개관절샘플 통과. CUA에서320/390/768px 두레슨14단계(총42상태)마다3개설명·16.5px·활성버튼1개·가로넘침없음 확인. 실제재생에서80초/60초끝정지,다시재생,일시정지,끝구간탐색,자료연결 확인. 자동화 브라우저가 MutationObserver.observe Node 오류를로그에남겼으나 프로젝트소스에는해당API없음; 화면기능오류는재현안됨.
- 로컬서버8792가종료돼있어다시시작(세션81467). 미리보기 http://127.0.0.1:8792/media/golf3d/training.html?lesson=aiming&v=56 .

## 이전 작업 v55 — 60초 임팩트 3D 레슨 (로컬 구현 완료)

- 후속 가독성 개선: 전신/손목을 감싼 공통 시각 카드 안에 단계별 3개 설명 추가. 모형을 가리지 않는 하단 영역. 화면 내 글자11→16.5px·700 이상, 상세 설명14→21px·700, 상세 제목19→28.5px·800. lesson CSS/JS 쿼리55.1, SW ptgolf-v55.1. 320/390/768px의7단계에서3개항목·폰트·굵기·가로넘침 없음·JS오류 없음 검사 및 모바일 스크린샷 확인. 미배포.

- 사용자 요청: 조윤성프로 영상 du58mmLNMnQ의 60초 요약을 3D로 구현.
- `media/golf3d/lesson.html`, `lesson.css`, `lesson.js`: 60초 자막 재생, 7단계, 일시정지·탐색·다시 보기, 전신/손목 선택, 각 단계 원본 타임스탬프 연결. 음성 합성은 제공하지 않는다.
- 전신은 기존 CMU 실측 예시를 타임라인으로 제어. 별도 손목 3D는 한 그립을 감싼 손가락, 고정 길이 팔, 오른손목 힌지와 점진적인 왼손목 펴짐을 설명한다. YouTube 인물의 동작 추출이나 관절각 측정값이 아니며 화면과 SOURCE.md에 명시.
- `viewer.js`에 외부 setPhase 제어만 추가, 기존 동작 데이터와 관절 계산은 그대로. 기존 3D 상단에 60초 레슨 링크 추가.
- 유튜브 자료에 원본 영상 4번째 추가(상단 노출), 자료 상세→3D→원본/자료로 돌아오기 연결. 영상 수 표시는 동적. 기존 자료의 YouTube 링크도 watch URL로 통일.
- 자산 v55, SW ptgolf-v55. 새 HTML/CSS/JS 선캐시. seed33 및 사용자 데이터 키/원문 변경 없음.
- 검증: `tests/golf-lesson.cjs` 320/390/768px 7단계×2시점, 재생/정지/탐색/다시 보기/키보드/오프라인, 실제 60초 재생 완료. `tests/golf-3d.cjs` 4,004포즈·384시점·연속성·터치·오프라인 통과. `tests/golf-hub.cjs` 기존 메모/레슨/질문/채택/검색/저장실패/오프라인 통과. 오류 없음. 이후 단계 버튼/재생 시 화면 스크롤 추가는 CUA 모바일 실제 확인.
- 로컬 미리보기: http://127.0.0.1:8792/media/golf3d/lesson.html?v=55 . Python 서버 세션76883. 이 변경은 아직 배포하지 않았으며 라이브는 v54.
- QA 이미지: C:/Users/peopl/Documents/PT-GOLF-samples/lesson-v55-wrist.png 및 lesson-v55-body.png. 검증 JSON docs/visuals/golf-lesson-verification.json.

## 0. 이전 v54 — 실측 스윙 재교정·배포 완료

- 사용자 피드백: v53 인물 형태로 바꾼 뒤에도 동작이 부자연스럽다. v53은 배포 승인 없이 로컬에만 있는 상태이며 사용자가 다시 수정을 요청했다.
- 원인: 임의 키 자세 사이에서 매번 속도가0으로 떨어지는 보간, 몸통과 별개인 고정 팔꿈치 방향, 단계/재생 전환 때 달라지는 자동 구도. 숫자 검증만으로 자연스러움을 판단했던 v53 검토는 불충분했다.
- **CMU Graphics Lab subject64 trial01의 실제 몸·클럽 광학 마커**를 사용하도록 동작 생성을 다시 교체했다. `motion.js`에120Hz·281프레임·2.333초 동작을 선탑재. 단계를 선택해도 같은 연속 타임라인의 해당 위치를 보여준다.
- `tools/build_golf_mocap.py`: 원본 C3D→축 변환·5프레임 노이즈 필터·구간 추출→번들 데이터. 원본 SHA와 세부 사용 조건은 `media/golf3d/SOURCE.md`. 원본은 사용자의 검토용 임시 폴더에 있으며 실행 시 재다운로드하지 않는다.
- 팔꿈치·무릎은 **측정된 굽힘 방향 + 고정 길이**로 피부 마커의 길이 변화를 보정한다. 손목·발 궤적, 머리 방향, 클럽 회전은 실측 데이터에서 연결한다. 오른손잡이의 타깃 방향은+X. 이전 임의 키 자세 골프 엔진은 남기지 않는다.
- 재생 중/일시정지/단계/슬라이더 모두 같은 전체 구도를 유지한다. 실제 템포·느리게(기본0.35배)·아주 느리게0.15배. 피니시에서 정지한다. 회전·핀치·키보드는 유지.
- **4개 클럽별로 따로 촬영된 스윙이 아니다.** 같은 실측 공통 스윙을 균일 축척으로 적용하며 클럽 헤드·티 표시가 다르다. 사용자 스윙 재현이나 전문 코칭의 정답으로 주장하지 않는다. 화면에 공통 예시 및 CMU 출처를 표시했다.
- seed33, 원문·2컷·개인 메모·레슨·유튜브 연결은 보존. 새 자산 버전54 및 SW ptgolf-v54에 `motion.js`까지 선캐시한다.
- 검증: 고정 관절/손·클럽 연결/단계 경계의 연속 속도/고정 구도/4,004포즈/384단계·시점/320·390·768px/재생·터치·오프라인 통과. 기존47개·원문1233·이미지94·모델47·메모·핀치·오프라인도 통과.
- 실제 스크린샷 비교와 연속 재생 확인. 로컬 검토 영상: `C:\Users\peopl\Documents\PT-GOLF-samples\golf-v54-qa\swing-motion-v54.webm`. 대표 비교 이미지를 `docs/visuals/golf-3d-review.webp`로 갱신.
- **배포 완료(2026-09-13)**: 사용자가 “배포해줘”라고 명시적으로 승인하여 `3aa2f686ffc47f449b87459cd26be50273d9a8ee`까지 main에 정상 푸시했다. Pages 실행 `34704927723` 성공. index·app·registry·SW·골프 뷰어 HTML/CSS/JS·포즈·실측 데이터·seed의 10개 공개 파일이 검증한 로컬 파일과 일치했다.
- 라이브390px에서 새 3D 로딩·재생·피니시 정지(`phase=1`, `playing=false`, 다시 재생)를 확인했다. 실제 사이트: https://peoplenhpark.github.io/PT-GOLF/?v=54#exercise/golf_driver . 전용 로컬 서버8792(세션58933)는 종료했다.

## 0-1. v53 — 스윙 노트 3D 첫 교체 (미배포·동작 재수정됨)

- 사용자 요청: 골프 스윙 노트의 기존 3D가 해독하기 어려운 비정상 형태여서 전면 교체.
- 드라이버·5번·7번·P 아이언 4종을 `media/golf3d/`의 **골프 전용 인물·포즈·뷰어**로 교체했다. 옷을 입은 연속된 몸통, 단순한 팔·다리, 왼손 장갑·오른손, 클럽·공·타깃을 구분한다. 기존 근육 조각 겹침 표현은 골프에서 사용하지 않는다.
- 준비 상태에서 정지해 시작. **준비 → 백스윙 → 탑 → 체중이동 → 손 내리기 → 임팩트 → 팔로스루 → 피니시** 8단계 버튼. 단계 선택은 일시정지하고 자세를 크게 맞춘다. 재생 중에는 스윙 전체가 들어오는 고정 구도를 쓰며 피니시에서 멈춘다. 처음 자세로 역재생하지 않는다.
- 앞·옆(타깃선 뒤)·뒤·사선, 회전·핀치·키보드, 슬라이더, 느린 재생 2속도. 기구 숨김은 골프에서는 제공하지 않는다.
- 두 손은 한 그립을 공유하며 팔·다리·클럽 길이가 고정된다. 클럽별 길이·스탠스·공 위치·헤드·티가 다르다. 3D는 동작 흐름 예시이며 실제 개인 모션캡처/스윙 분석이 아니다.
- `media/3d/poses.js`의 구형 골프 분기를 제거했다. 구형 골프 viewer URL은 새 전용 viewer로 이동한다. PT 포즈·렌더러는 골프 주소 이동 처리 외에 보존.
- seed33, 운동 날짜·원문·2컷·메모·즐겨찾기·레슨·영상 연결은 유지한다. 자산/SW 버전은53. 골프 전용 HTML/CSS/포즈/뷰어를 선캐시하므로 아직 열어보지 않은 클럽의 3D도 셸 설치 후 오프라인에서 열 수 있다.
- 검증: `tests/golf-3d.cjs`의 **4,004개 포즈**, 고정 관절·그립/샤프트·공 접점·연속성, **320/390/768px · 384개 단계/시점**, 재생/정지/끝/재시작, 터치, 구형 링크, 오프라인 통과. 결과 `docs/visuals/golf-3d-verification.json`.
- `tests/visual-media.cjs` 전체47개·원문1233·이미지94·모델47·메모·핀치·오프라인 통과. 골프 학습 허브 테스트도 통과. 골프 포즈의 수학 검사는 새 전용 테스트로 옮겼으며 PT의 기존 검사를 약화하지 않았다.
- 모바일 4클럽 × 8단계를 실제 스크린샷으로 비교했다. 대표 검토 이미지 `docs/visuals/golf-3d-review.webp`; 전체 로컬 증거 `C:\Users\peopl\Documents\PT-GOLF-samples\golf-v53-qa`.
- **당시 배포 보류 이력**: v53 커밋 `7bec656`의 main 푸시는 명시적 배포 승인 미확인으로 자동 심사에서 거절되었다. 이후 사용자가 동작 재수정을 요청하여 v54로 교체했고, 별도 “배포해줘” 승인 후 v54가 배포되었다. 현재는 미해결 차단이 아니다.
- v53은 독립적으로 배포하지 않았으며 현재 v54로 대체되었다. 이전 로컬 검토 서버는 종료했다.

## 0-2. v52 — 골프 학습 허브 (유지되는 기능)

- 사용자 요청: 기존 골프 / 예정된 개인 레슨 / 지정 유튜브3편을 유기적으로 연결. 구현·로컬 검증·Pages 배포·실제 모바일 화면 확인 완료.
- 골프는 **스윙 노트 · 레슨 · 유튜브** 3탭. 기본 진입은 스윙 노트이며 전체/드라이버/아이언 분류를 유지한다.
- 원문47개, 기존 id·메모·즐겨찾기·2컷·3D·seed33은 보존. 운동 갱신일도 무변경.
- 새 파일: `js/golf-data.js`(주제·영상·공식 레슨), `js/golf.js`(학습 허브), `css/golf.css`, `tests/golf-hub.cjs`.
- 영상: `0EgzSDUsKvg` 상하체 순서(심승룡), `uvgnUl93Twg` 들린척추 세팅(쇼츠골프), `RSbjGWhzEnQ` 팔 사용(세계유명 골프정보).
- 첫째/셋째는 YouTube 자동자막, 둘째는 화면 내 5단계 문구를 직접 읽어 요약. 둘째 4단계는 **클럽이 뒤로 처지지 않도록**이며 오른발 뒤꿈치 유지로 오인하지 않는다. 세팅과 지연의 개인 적용은 다음 레슨 질문으로 연결했다.
- 영상을 시청한 것만으로 기존 코칭을 덮어쓰지 않는다. 각 영상에 핵심 구간 링크·주제·관련 노트·내 적용 메모·적용 상태가 있다. 원본 YouTube 열기를 주 버튼으로 제공하고, 앱 내 재생은 클릭 시에만 iframe을 만든다. 외부 재생은 네트워크/YouTube 정책에 의존한다. 인앱 브라우저의 임베드 재생은 확인되지 않았으므로 정상 재생을 단정하지 않는다.
- **영상/노트 → 질문 저장 → 레슨에서 답변받은 질문 선택 → 출처 자동 연결 → 내 연습에 반영** 흐름. 레슨은 날짜·코치·문제·교정·숙제·차이·결과와 관련 노트/영상/주제를 가진다. 질문 선택 시 출처 영상/노트가 자동 연결된다.
- 개인 레슨을 가공해 만들어 넣지 않았다. 공식 `GolfContent.lessons`는 현재 빈 배열. 후속 실제 레슨을 반영할 때 안정된 id·실제 레슨 날짜·noteIds·videoIds·topics를 기록한다.
- 골프 기기 기록 키 **`ptgolf_learning_v1`**: `{lessons, questions, focus, videoNotes}`. 기존 `ptgolf_overlay_v1`, 캘린더와 별개이며 자동 동기화 없음. JSON 내보내기/가져오기 UI는 복원하지 않았다.
- `focus`는 사용자가 선택한 핵심 최대3개. 원본 출처와 연결할 노트 id를 보존하고, 해제 시 삭제 대신 `active:false`로 과거 이력을 남긴다. 기존 운동 코칭을 자동 덮어쓰지 않는다.
- 상세 상단의 **관련 레슨·영상 바로 보기**는 하단 연결 구간으로 스크롤한다. 기존 움직임·느낌 → 2컷 → 3D 순서를 보존한다.
- 주제 페이지는 같은 주제의 노트·개인 레슨·영상을 함께 보여준다. 골프 전체 검색은 기존 노트/메모·레슨·영상/메모를 함께 검색한다.
- 링크: `#golf/notes`, `#golf/lessons`, `#golf/videos`, `#golf/videos/<id>`, `#golf/lessons/<id>`, `#golf/topic/<URI 인코딩 주제>`. 기존 `#exercise/<id>`도 유지.
- 저장 실패 시 성공 메시지를 내지 않고 입력/기존 저장 데이터를 보존한다. 읽기 실패한 골프 키를 빈 데이터로 덮어쓰지 않는다.
- v52 검증: 전체47개/원문1233/이미지94/모델47/메모/핀치/3D터치/오프라인 모두 통과. 새 골프 검사는 질문↔레슨↔영상↔노트, 날짜 보존, 채택 이력, 최대3개, 검색,320·390·560px, 다크·라이트, 저장 실패, 오프라인 메모를 검사한다.
- 실행: 7절 NODE_PATH 설정 후 `node tests/golf-hub.cjs`. `tests/visual-media.cjs`는 이제 SW에서 현재 버전을 읽는다.
- 배포 검증: 앱 커밋 `01b958d`, Pages 실행 `34697366356` 성공. 공개된 index·골프 CSS/JS/데이터·app·SW·seed·3D viewer의 8개 파일이 검증한 로컬 파일과 일치했다. 실제 라이브390px에서 3탭, 영상→드라이버 노트→관련 자료 바로가기, JS 오류 없음을 확인했다. 전용 미리보기 서버8792는 종료했다.
- 배포 URL: https://peoplenhpark.github.io/PT-GOLF/?v=52#golf/notes
- 다음: 실제 골프 레슨 자료가 오면 새 구조에 반영. PT 케이블 푸시다운 세팅 이월 사항은9절 유지.

## 0-3. 이전 v51 완료 상태 (이력)

**최신 확정: 사용자가 케이블 푸시다운 배치를 확인하고 전체 적용을 승인했다. 전체47개 운동은 움직임·느낌 → 2컷 → 3D 모델 순서다. 원문·메모·이미지·3D 동작은 그대로다.**

- 사용자 요청 **“케이블 푸시다운을 표준으로 나머지 동작들도 2컷+3d 삽입”**은 구현·검증·push·라이브 확인까지 완료했다.
- **47개 운동 = PT43 + 골프4**. 모든 운동에 2컷과 실제 회전·재생 가능한 3D가 연결된다.
- 새 이미지 **46세트/92컷 WebP**, 기존 푸시다운 **2컷 PNG**를 합쳐 총94컷. 새 WebP 합계 **3,338,454바이트(약3.34MB)**.
- 원문 코칭, 원칙, 메모 및 기존 운동 갱신일은 시각 자료 적용 때문에 변경하지 않았다. `data/seed.json`은 앱 배포 커밋에서 무변경.
- 기존 `js/store.js`의 SVG 경로 이행 보조 코드도 `5873b97`에 포함됐다. 미커밋 작업으로 오해하지 말 것.
- 앱은 GitHub Pages에서 배포 확인 완료. 이 핸드오프 요청 시작 시 git 작업 트리는 깨끗했고 `main`/`origin/main`이 같은 앱 커밋이었다.
- 현재 요청에 남은 구현 작업이나 배포 대기는 없다. 다음 사용자 피드백/녹취록을 기다리는 상태다.
- 이번 세션의 로컬 미리보기 서버(8791·8792)는 명령줄과 실행 경로를 확인한 후 종료했다. 다음 세션은 서버가 떠 있다고 가정하지 말고 7절 명령으로 새로 시작한다.
- 다음 녹취록에서 가장 먼저 확인할 이월 항목은 **케이블 푸시다운의 중량·손잡이·상체 각도·횟수/세트**다(9절).
- 다음 작업 시작: 저장소 확인 → 9절 이월 항목 → 실제 대상 소스 읽기 → 사용자 요청 범위의 작업만 진행. 이미 완료한 전체 이미지 작업을 다시 시작하지 않는다.

바로 확인할 주소:

- 홈: https://peoplenhpark.github.io/PT-GOLF/?v=53
- 데드버그: https://peoplenhpark.github.io/PT-GOLF/?v=53#exercise/pt_deadbug
- 케이블 푸시다운: https://peoplenhpark.github.io/PT-GOLF/?v=53#exercise/pt_pushdown
- 골프 드라이버: https://peoplenhpark.github.io/PT-GOLF/?v=53#exercise/golf_driver

## 1. 프로젝트와 실제 작업 위치

**PT노트**는 운동 중 한 손으로 코칭을 읽고 체크·메모하는 개인용 PWA다. 순수 HTML/CSS/JavaScript이며 빌드 단계, 서버 계정, 로그인, 백엔드가 없다. GitHub Pages의 프로젝트 하위 경로 `/PT-GOLF/`에서 실행된다.

- 실제 git 루트: **C:\APARK\PT_GOLF**
- remote: **https://github.com/peoplenhpark/PT-GOLF.git**
- 배포 브랜치: **main**. push하면 Pages가 배포한다. 일반적으로 빠르지만 고정된 완료 시간을 보장하지 않는다.
- **C:\APARK\PT_GOLF\새 폴더**는 오래된 스테이징 사본이며 gitignore 대상이다. 여기를 수정하면 앱에 반영되지 않는다.
- `CLAUDE.md`와 `docs/HANDOFF.md`는 초기 맥락이다. 거기에 남은 SVG·내보내기·환경 관련 옛 지침보다 이 문서의 현재 기준을 우선한다.
- 코드 변경에 필요한 commit/push는 이 프로젝트의 기존 승인된 작업 흐름이다. 별도 저장소에서 쓰는 push 제한을 이 프로젝트에 적용하지 않는다.

```text
C:\APARK\PT_GOLF\
  index.html                  앱 셸, 모달, viewport, 버전 붙은 스크립트
  css/style.css               테마·모바일·2컷·3D 영역
  js/store.js                 seed + localStorage 병합, CRUD, 검색, 날짜·캘린더
  js/app.js                   화면 렌더, URL 라우팅, 이미지·3D 연결, 메모
  js/exercise-media.js        47개 운동의 이미지 경로·캡션·3D 종류
  media/golf3d/              골프4개 실측 동작 데이터·포즈·8단계 뷰어 (v54)
  data/seed.json              코칭 영구 원본: v33, 47운동, 10원칙
  sw.js                      앱 셸/뷰어 선캐시, 네트워크 우선·오프라인 복구
  manifest.webmanifest       PWA scope/start_url (프로젝트 하위 경로 유지)
  media/3d/poses.js           46개 운동의 관절 위치·기구·단계
  media/3d/viewer.js          인체/근육 표시·기구 렌더·카메라·재생
  media/3d/viewer.html        공통 iframe 뷰어
  samples/pushdown-3d/        기존 푸시다운 전용 뷰어·2컷·Three.js·라이선스
  docs/images/guides/         새 WebP 92개 (git 배포)
    source/                  생성 원본 PNG46개 (로컬 보관, gitignore)
  docs/images/*.svg          기존 일러스트 원본, 삭제하지 않음
  docs/visuals/              생성 프롬프트·패널 순서·검증 결과·미디어 설명
  docs/AGENT_HANDOFF.md       이 문서
  tests/visual-media.cjs      전체 운동·메모·모바일·오프라인 검증
  tools/svgcheck.py           기존 SVG 검사 도구
  tools/jsbal.py              Node가 없을 때의 보조 괄호 검사
  reference/                 과거 SVG/PDF 제작 자료
```

## 2. 사용자 확정 UI와 보존할 동작

전체 운동 상세 순서는 다음과 같다. 없는 필드는 생략한다.

1. 뒤로가기·운동명·카테고리 태그·갱신일·즐겨찾기/수정
2. 단계 키워드(골프), 스펙(중량/횟수/핵심), 준비 자세
3. **타깃 근육·움직임·느낌**
4. **준비/동작 2컷 이미지와 읽을 수 있는 텍스트 설명**
5. **“입체로 자세 보기” 버튼 하나** — 누르면 3D 펼침
6. 운동 중 핵심 체크리스트 → 잊지 말 것 → 해당 카테고리 원칙
7. **내 메모**와 편집 기능

지켜야 할 결정:

- 이미지와 3D를 추가하면서 하단 메모·원칙·기존 큐를 없애거나 요약본으로 대체하지 않는다.
- 그림 확대 방법 안내 문구, “입체로 자세 확인하기” 같은 부제, 3D 소개 설명은 별도로 넣지 않는다.
- **2컷은 브라우저 기본 전체 화면 핀치 확대**. viewport는 `user-scalable=yes, maximum-scale=5`. 과거 라이트박스는 사용자가 불편해해 원복했으므로 되살리지 않는다.
- **PT 3D 안에서는** 한 손가락/드래그 회전, 두 손가락/휠 확대. 앞·옆·뒤·처음 시점, 재생/일시정지, 동작 슬라이더, 속도, 기구 표시를 지원한다.
- 카테고리 칩은 **flex-wrap으로 화면 안에 줄바꿈**. 가로 스크롤 탭으로 되돌리지 않는다. 긴 칩의 ` · ` 구분은 두 줄 라벨로 표시한다.
- 본문 글자 확대는 `text-size-adjust:125%`. 예전 SVG 내부 글자1.3배 지침은 보관 SVG에만 해당하며 새 캡션은 HTML/CSS 텍스트다.
- 내보내기/가져오기 UI는 사용자 요청으로 제거했다. Store 내부 함수가 남아 있어도 UI를 다시 만들지 않는다.
- 홈 검색과 PT/골프/즐겨찾기 헤더 검색을 유지한다. 카테고리 태그를 눌러 해당 탭으로 이동하는 기능도 유지한다.
- 예전 **골프 그림 없음**, **뒤꿈치 받침 스쿼트 그림 불필요** 결정은 이번 **전체 운동** 적용 요청으로 변경됐다. 단, 골프 핵심 키워드 스트립은 그대로 유지한다.
- 3D를 닫거나 운동 상세를 나가면 iframe을 제거한다. 모든 모델을 홈에서 미리 실행하면 안 된다.

## 3. 데이터와 저장소 동작

`data/seed.json`의 구조:

```jsonc
{
  "version": 33,
  "updated": "...",
  "parts": [{ "id": "pt", "label": "PT", "icon": "🏋️" }],
  "principles": [{
    "id": "pr_xxx", "part": "pt", "scope": "등",
    "title": "...", "items": ["..."], "reminders": ["..."]
  }],
  "exercises": [{
    "id": "pt_xxx", "part": "pt", "category": "등", "name": "...",
    "spec": "중량 · 반복수 × 세트 또는 한 줄 핵심",
    "prep": ["준비 자세"], "steps": ["단계"],
    "focus": { "muscle": "타깃", "move": "움직임", "feel": "느낌" },
    "cues": ["운동 중 핵심"], "reminders": ["잊지 말 것"],
    "image": "docs/images/기존.svg",
    "memo": "", "favorite": false, "updated": "YYYY-MM-DD"
  }]
}
```

- **원본은 seed, 기기 편집은 localStorage**. `ptgolf_overlay_v1`의 `{overrides, deleted}`를 `Store.getAll()`에서 합친다. 서버 동기화는 없다.
- 브라우저 데이터 전체 삭제는 메모·즐겨찾기·개별 수정·캘린더를 잃을 수 있다. 단순한 구버전 문제를 해결하려고 localStorage 초기화/삭제를 실행하지 않는다.
- seed 항목은 기존 id를 유지한다. 같은 운동을 새 id로 다시 만들면 기존 메모/즐겨찾기와 분리된다.
- 사용자가 전체 운동 객체를 저장한 오래된 overlay도 있다. 이 경우 seed의 새 필드 일부가 기존 로컬 값으로 가려질 수 있다. 읽기 구조를 확인하고 사용자의 편집을 일괄 삭제하지 않는다.
- 시각 자료 registry가 있는47개는 `e.image`보다 2컷+3D가 우선한다. 사용자 추가 운동 등 registry가 없는 항목은 기존 focus/image 상세로 동작한다.
- `getPrinciple`은 같은 part 중 **카테고리 정확 일치 → scope="*"** 순서. 이 우선순위를 바꾸면 재활 등 전용 원칙이 가려진다.
- 검색은 여러 공백 단어의 AND. 이름·스펙·카테고리·prep·focus·steps·cues·reminders·memo를 모두 조회한다.
- 체크리스트 체크 상태는 해당 실행 중 메모리 상태이며 새로고침하면 초기화된다. 개인 메모와 혼동하지 않는다.
- 캘린더는 별도 localStorage에 예약/완료/휴무와 예약 시각을 저장한다.
- **최근 일주일 세션은 별도 운동 수행 로그가 아니라 `getAll()`의 `updated`를 날짜별로 집계한 화면이다.** 오늘 포함7일, 최신순, 해당 기간 항목이 없으면 섹션 자체를 숨긴다.
- 현재 `upsert`는 `updated`를 오늘로 바꾸고 메모·즐겨찾기 변경도 이를 사용한다. 따라서 로컬 메모/즐겨찾기 변경이 최근 날짜 목록에 영향을 줄 수 있다. 이는 기존 동작이며 이번 시각 자료 작업에서는 바꾸지 않았다.
- 녹취를 seed에 반영할 때는 운동의 `updated`를 **레슨 날짜**로 기록한다. 이미지·문서만 바꿨다고 모든 운동 날짜를 오늘로 바꾸지 않는다.

PT 카테고리 현재 순서: **등 / 팔 / 가슴 / 어깨 / 하체 / 하체 · 머신 / 일일 수행 / 하체 재활 · 일일수행2 / 코어 · 밸런스**. 골프는 드라이버·아이언이다. `추천운동`은 `코어 · 밸런스`로 통합/개명했다.

## 4. 2컷과 3D 구현을 수정하는 법

### 연결 구조

`js/exercise-media.js`의 `window.ExerciseMedia[exerciseId]`에는 `name, kind, captions, notes, focus, target, images, viewer`가 들어간다. 푸시다운은 기존 전용 함수/뷰어를 쓰므로 일부 필드 구성이 다르다.

- 앱: `focusHtml` / `exerciseMediaHtml` / `pushdownMediaHtml` / `bindExercise3D`.
- URL: `#exercise/<id>`. 잘못된 id는 정상 운동 화면으로 취급하지 않는다.
- 일반 iframe: `media/3d/viewer.html?exercise=<id>&v=51`.
- 푸시다운 iframe: `samples/pushdown-3d/viewer.html?v=52`.
- iframe 높이는 `ptgolf-viewer-height` 메시지로 전달한다. 부모는 **origin, source===해당 iframe.contentWindow, 유한한 높이**를 검증하며320~1400 범위로 제한한다.
- 3D는 로컬 `samples/pushdown-3d/three.min.js`(Three.js0.160.1)를 공유한다. MIT 라이선스 파일도 같은 폴더에 있다.
- `poses.js`는 Three.js에 의존하지 않는 좌표 함수다. x=좌우, y=위, z=앞, 단위는 미터. 관절·기구·카메라 기준점·동작 단계를 반환한다.
- `viewer.js`는 재사용하는 메시 풀로 인체와 기구를 그린다. 타깃은 붉게 표시하고 사용자 시점과 재생 상태를 관리한다.
- 확인용 `window.exerciseViewer.snapshot()`은 id/kind/phase/pose/yaw/distance/meshCount의 사본을 반환한다. 자동 검사에서 사용하며 사용자 데이터는 포함하지 않는다.
- 골프4개는 같은 스윙 엔진을 공유하되 클럽 길이·스탠스·클럽 헤드를 구분한다. 오른손잡이 예시다.

**주의:** registry의 `notes/focus/captions`는 seed와 별도로 저장된 제작 당시 문구다. seed나 로컬 메모를 바꾼다고 3D 설명·그림이 자동 재생성되지는 않는다. 새 녹취로 자세·큐를 바꾸면 **seed, registry 문구, 2컷, poses.js** 사이의 일치 여부도 확인해야 한다. 현재 메인 focus·체크·메모는 Store의 값을 계속 렌더한다.

### 유지해야 할 개인 코칭 변형

- **데드버그:** “끝까지 뻗지 말고 반쯤, 약40%만”. 허리가 뜨기 직전까지만, 배는 계속 조인 상태. 2컷의 움직이는 무릎도 굽힌 채로 짧게 표현했다. 초기 생성본의 과도한 뻗기를 교정했다. 40% 표시는 개인 관절 각도를 측정한 수치가 아니다.
- **SLR:** 들지 않는 다리는 무릎을 세우고 발로 지지. 드는 다리는 편다.
- **티밸런스:** 지지 다리와 뒤로 뻗는 다리를 곧게, 골반이 열리지 않게. 3D의 과한 무릎 굽힘을 보정했다.
- **덤벨 풀오버:** 무릎을 몸쪽에 당겨 고정한 변형. 임의로 양발 바닥 버전으로 교체하지 않는다.
- **이상근 스트레칭:** 앞다리를 벤치에 접어 올리고 뒷다리를 빼 상체를 숙이는 버전. 옛 바닥 figure-four가 기본이 아니다.
- **백 익스텐션+덤벨 로우:** 몸통을 일자 사선으로 버틴 채 덤벨을 수직으로 당기는 변형. 양쪽2컷 모두 몸통 일자를 유지하도록 재생성했다. 머리·시선은 아래로(9/1 교정이 이전 “머리 들기”보다 최신).
- **암컬:** 원판이 수직으로 오르는 코칭을 반영. 일반 반원 궤적만 구현하지 않는다.
- **머신 로우:** 발판에 다리를 올려 골반을 고정하는 해머 스트렝스 버전. 케이블 시티드로우와 별도 운동이며 기구도 구분한다.
- **레그 익스텐션:** 현재2컷/3D는 한 발 버전. 원문에는 양발/한 발 병행, 무릎을 골반 쪽으로 들어 올리는 느낌을 기록했다.
- **레그 프레스:** 넓은 스탠스와 안쪽 허벅지 감각. 생성 원본의 좌우를 뒤집어 준비→미는 동작 순서로 저장했다.
- **사이드 레터럴 레이즈:** 양팔을 어깨 높이까지만. 두 번째 컷에 양팔 전체가 보이도록 교정했다.
- **골프:** 골반 접기 → 백스윙 → 왼쪽 체중이동하며 손 내리기 → 채가 내려올 때까지 지연 → 몸통 회전/면 임팩트 → 팔 펴기/중심 확인. 어깨 턴 높이·머리/몸 중심에 관한 기존 설명도 유지한다.

모델은 관절 동작을 관찰하는 **단순화된 인체**다. 실제 사용자의 신체·기구 치수, 모션캡처, 임상적으로 검증된 개인 자세를 재현한 것은 아니다. 좌표 검사 통과를 모든 운동의 완벽한 생체역학 인증으로 표현하지 않는다. 개별 운동 후 사용자 피드백으로 교정하는 흐름을 유지한다.

### 이미지 제작·재현

- 내장 `image_gen`으로 운동별2컷 한 장씩 생성했다. 임의로 유료 CLI/API 생성 경로로 전환하지 않는다.
- 스타일: 차콜 배경, 은색 인체 또는 회색 운동복, 붉은 타깃 근육 위치. 동일한 준비/동작 시점과 자연스러운 비율. 캡션은 그림 안에 굽지 않고 HTML로 표시한다.
- 최초46개 프롬프트: `docs/visuals/exercise-image-prompts.json`.
- 최종 원본 PNG46개: `docs/images/guides/source/<id>.png`. **gitignore이므로 새 clone에는 없다.** 최적화된 WebP는 모두 git에 있다.
- 초기 프롬프트만 다시 실행하면 최종 교정본과 같아지지 않을 수 있다. 위 변형과 데드버그/백 익스텐션/레터럴 레이즈 교정 조건을 함께 적용하고 직접 확인한다.
- 패널 순서: `docs/visuals/panel-order.json`. 현재 레그 프레스만 `[1,0]`, 나머지는 `[0,1]`.
- 분리 방식: 원본 폭의 절반을 왼쪽/오른쪽 패널로 사용, 홀수 폭이면 나머지는 오른쪽. 각 패널은 최대폭700px(작은 원본은 확대하지 않음), WebP quality86.
- 최종 경로: `docs/images/guides/<id>-start.webp` / `<id>-end.webp`.
- 실제 분리·검토용 로컬 도구: `C:\Users\peopl\Documents\PT-GOLF-samples\build-all-media.cjs`. Sharp 경로와 저장소 경로가 이 PC에 고정되어 있다. 레그 프레스 순서는 코드에도 반영돼 있다. 새 환경에서는 이 문서의 규칙과 panel-order를 기준으로 재구성한다.
- 원본을 검토하고 모바일 화면에서 캡션과 컷 순서가 맞는지 확인한 뒤 반영한다. 기존 SVG, 원본 생성 파일, 사용자 자료를 정리 명목으로 삭제하지 않는다.

## 5. 녹취록 반영 규칙

2026-08-11부터 트레이너가 바뀌었다. 새 트레이너는 중량보다 정렬·통제, 어느 근육을 어떻게 쓰고 느끼는지를 강조한다. 사용자도 **준비 자세 / 운동 중 핵심 / 중량·횟수**를 특히 중요하게 본다.

1. 파일명만 보고 반영하지 않는다. 날짜·내용·직전 입력과의 동일 여부를 먼저 확인한다. 사용자가 잘못 고른 동일 파일은 작업을 중지해 달라고 한 이력이 있다.
2. 타 회원 레슨이 함께 녹음될 수 있다. 주인 세션 종료 인사와 화자 전환 등을 확인해 다른 회원의 내용을 제외한다. 7/24 후반부에 이런 사례가 있었다.
3. 기존 id, prep/cues/reminders/focus/spec와 대조한다. 같은 말은 추가하지 않고 기존 문장을 고친다. cues↔reminders 사이 중복도 확인한다.
4. 상충하면 **최신 레슨 내용이 우선**이다. 수치도 최신으로 갱신한다. 동작 변형을 남길 때는 기본 자세와 구분하여 간결하게 적는다.
5. 사용자 최신 요청이 “보강”이면 보강, “전면 대체”이면 **그 세션에 나온 운동**의 코칭을 새 내용으로 대체한다. 과거의 대체 요청을 근거로 매번 전체 데이터를 무조건 덮어쓰지 않는다.
6. 신규 운동은 기존 운동과 다른 동작/기구인지 확인하고 카테고리를 선택한다. 기존 동작과 이름만 달리 중복 등록하지 않는다. 고블릿 스쿼트 명칭은 사용자가 확정했다.
7. `prep`=준비, `cues`=실행 핵심, `spec`=중량·반복수·세트, `focus`=근육·움직임·느낌으로 정리한다. 준비 필드가 없는 기존 운동은 새 녹취에 등장할 때 채운다.
8. 중요한 내용을 쉽게 강조하라는 요청은 상단 큐·focus·필요한 spec·2컷/3D까지 일관되게 반영한다. 데드버그40%가 대표 사례다.
9. `updated`는 해당 레슨 날짜로 기록하고 seed 버전을 갱신한다. 시각 자료/registry와 동작이 충돌하지 않는지도 확인한다.
10. 유사 문구 스캔은 후보를 찾는 보조 수단이다. 점수만 낮추려고 필요한 코칭을 삭제하거나 다른 의미를 합치지 않는다.

## 6. 자산 버전과 배포

현재 릴리스 **N=65**. 앱/이미지/3D 변경 시 다음 버전은66이지만, 사용자가 그 사이 변경했을 수 있으므로 작업 시작 때 실제 파일을 확인한다.

함께 맞출 곳:

- `sw.js`의 `CACHE = 'ptgolf-vN'`와 ASSETS의 버전 쿼리.
- `index.html`의 **css/style.css, js/store.js, js/exercise-media.js, js/app.js** 쿼리4개.
- `js/app.js`의 `ASSET_VER`.
- `index.html`의 `css/golf.css`, `js/golf-data.js`, `js/golf.js` 및 SW ASSETS도 함께 갱신.
- `media/3d/viewer.html`의 **exercise-media.js, poses.js, viewer.js** 쿼리.
- `media/golf3d/viewer.html`의 **viewer.css, motion.js, poses.js, viewer.js** 쿼리 및 SW의 해당 5개 자산.
- `samples/pushdown-3d/index.html`의 앱 바로가기 쿼리 및 문서의 대표 최신 링크.
- 푸시다운 전용 `pushdown.js`/`three.min.js`는 현재 쿼리 없이 로드된다. 새 SW 캐시 및 네트워크 우선 정책으로 관리한다. 이 파일 자체를 수정할 때는 실제 새 코드 로딩까지 확인한다.

옛 **“5곳만 bump” 스니펫은 더 이상 충분하지 않다.** 읽지 않은 파일에 무차별 정규식 치환을 하지 말고 위 경로를 확인한다.

- **seed 콘텐츠만** 변경하면 날짜 쿼리로 새로 받아오므로 JS/CSS 자산 버전 bump는 필수 아님. 다만 시각 자료도 바꿨다면 자산 버전을 올린다.
- **핸드오프 등 문서만** 수정하면 seed/자산/SW 버전은 그대로 둔다.
- GitHub 프로젝트 사이트이므로 앱 URL과 파일 참조는 상대경로를 유지한다.
- 변경 확인 → 관련 검증 → 의도한 파일만 stage → `git diff --cached --check` → 한국어 커밋 → `git push origin main` → 라이브 확인.
- remote가 앞서 push가 거절되면 fetch 후 차이를 확인하고 정상 merge/rebase 흐름으로 통합한다. 강제 push로 덮어쓰지 않는다.
- git identity가 이미 있으면 바꾸지 않는다. 과거 author는 `apark`였다.

캐시 동작:

- SW는 네트워크 우선, 성공 응답만 캐시. 실패 시 정확한 URL 캐시 → 동일 origin에서 쿼리 무시 캐시 → 탐색 요청만 index fallback.
- 셸·seed·공통3D코드·Three.js·기존 푸시다운 자산은 선캐시한다. **새 큰 이미지는 방문 시 캐시**하므로 처음 보려는 이미지에는 연결이 필요하다.
- 새 SW는 skipWaiting/clients.claim. 앱은 updateViaCache:'none'로 SW를 갱신하고 기존 제어기가 있던 화면은 controllerchange 때 한 번 새로고침한다.
- Chrome에서 확인하는 것이 실제 사용 흐름이다. 과거 카카오 인앱에서 오래된 버전이 계속 보였고 “다른 브라우저로 열기 → Chrome”으로 해결했다. 웹뷰별 SW 지원·캐시 동작을 단정하지 않는다.
- GitHub 장애로 배포가 지연된 적이 있다. Pages 배포/Actions check 상태와 GitHub Status를 확인한다. 로컬 수정 오류로 오인해 불필요하게 코드를 바꾸지 않는다.
- 빈 커밋 재배포는 과거 장애 복구 후 사용자 요청으로 사용한 방법이다. 장애 동안 무한 재시도하지 않는다.

## 7. 이 PC의 실행 환경과 검증 재현

**중요: 로컬 서버 사용 가능.** 과거 샌드박스에서 불가능했던 기록은 현재 환경에 그대로 적용되지 않는다. 이번 세션은 승인된 실행으로 127.0.0.1 서버와 Chrome을 사용했다.

Windows PowerShell, 기본 작업 디렉터리는 C:\Users\peopl일 수 있으므로 매번 저장소 위치를 명확히 지정한다. 일반 sandbox 실행에서 read-only carveout 오류가 나면 승인된 실행 경로를 사용한다. 임의로 다른 작업/프로세스를 종료하지 않는다.

이 PC에서 확인한 런타임:

```powershell
$ptRuntime = 'C:\Users\peopl\.cache\codex-runtimes\codex-primary-runtime\dependencies'
$ptNode = Join-Path $ptRuntime 'node\bin\node.exe'
$ptPython = Join-Path $ptRuntime 'python\python.exe'
Set-Location 'C:\APARK\PT_GOLF'
$env:NODE_PATH = Join-Path $ptRuntime 'node\node_modules'
```

다른 PC/다음 환경에서는 해당 경로가 있는지 먼저 확인한다. 번들 런타임 위치가 바뀌면 환경의 의존성 조회 기능으로 찾는다. 시스템 HOME/CODEX_HOME 변수를 다른 용도로 덮어쓰지 않는다.

**터미널1 — 추적 가능한 전용 미리보기:**

```powershell
& $ptPython -m http.server 8792 --bind 127.0.0.1 --directory 'C:\APARK\PT_GOLF'
```

기동한 프로세스/세션 id를 기록하고 세션 종료 시 그것만 정리한다. 포트가 사용 중이면 실행 주체를 확인하며, “python 전체 종료”를 하지 않는다.

**터미널2 — 전체 검증:**

```powershell
$ptRuntime = 'C:\Users\peopl\.cache\codex-runtimes\codex-primary-runtime\dependencies'
$ptNode = Join-Path $ptRuntime 'node\bin\node.exe'
Set-Location 'C:\APARK\PT_GOLF'
$env:NODE_PATH = Join-Path $ptRuntime 'node\node_modules'
$env:PTGOLF_BASE_URL = 'http://127.0.0.1:8792/'
Remove-Item Env:SKIP_IMAGE_CHECK -ErrorAction SilentlyContinue
& $ptNode tests/visual-media.cjs
```

- 테스트는 Playwright와 설치된 **Chrome(channel:'chrome')**을 사용한다. Playwright 번들 Chromium이 없다고 중단하지 않는다.
- `SKIP_IMAGE_CHECK=1`은 이미지 미완성 시 임시 검사 옵션. 최종 릴리스에서는 해제한다.
- `SCREENSHOT_DIR`를 지정하면 전체3D 스크린샷을 저장한다.
- `docs/visuals/verification.json`은 테스트 성공 시 기록된다. 실패 후 남아 있는 파일을 방금 통과한 결과로 오해하지 않는다.
- 순수 좌표·관절 길이·개인 코칭 불변조건, 모든 상세의 원문·이미지·3D, iframe 열기/접기·회전, 메모·즐겨찾기·개별 편집, 네이티브 핀치·3D 터치, 방문한 운동의 오프라인 재접속을 검사한다.
- 핀치 테스트에서 **대상 페이지를 bringToFront한 다음 실제 touchStart/touchMove/touchEnd를 전달**해야 했다. 다른 테스트 탭이 활성화된 상태나 synthesizePinchGesture만으로 얻은 무변화는 앱 확대 불가를 뜻하지 않는다.
- 검증은 분리된 새 브라우저 컨텍스트를 사용한다. 사용자의 실제 Chrome 프로필·메모를 테스트 데이터로 덮어쓰지 않는다.
- 단순 문서 변경에는 전체 WebGL 테스트를 반복하지 않는다. 링크·사실·git diff 검증으로 충분하다.

기타 실행 팁:

- Python 한글 출력은 `-X utf8` 또는 `PYTHONIOENCODING=utf-8` 사용.
- 긴 Windows 명령은 길이 제한으로 실패할 수 있다. 긴 파일은 안전한 here-string/파일 기반 입력과 작은 청크로 쓴다.
- PowerShell의 큰따옴표, 백틱, 달러 식을 섞어 셸 문자열을 조립하지 않는다. PR/긴 커밋 본문은 파일 전달이 안전하다.
- 일부 로컬 view_image 호출은 샌드박스 경로 오류가 있었다. 당시 Sharp/System.Drawing의 검토용 contact sheet와 Chrome 스크린샷으로 확인했다.
- `tools/jsbal.py`는 보조 도구다. Node가 있는 현재 환경에서는 `node --check`와 실행 검증이 우선이다.
- 과거 임시 산출물 위치: `C:\Users\peopl\Documents\PT-GOLF-samples`. 새 checkout의 필수 의존성이 아니며, git에 있는 코드·WebP만으로 앱이 실행된다.

## 8. 완료된 검증 증거

앱 커밋 **5873b97**에 대한 최종 자동 검사 결과:

```json
{
  "exercises": 47,
  "sourceLines": 1233,
  "images": 94,
  "models": 47,
  "errors": [],
  "memoPreserved": true,
  "nativePinch": true,
  "modelTouch": true,
  "offline": true
}
```

- 결과 파일: `docs/visuals/verification.json`. 좌표 이상 목록 `docs/visuals/pose-check.json`은 `[]`.
- 46개 신규2컷은 모두 contact sheet로 직접 검토했다. 데드버그·백 익스텐션·레터럴 레이즈 등은 검토 후 교정했다.
- Chrome 모바일390px 기준 전체47개 상세의 원문1,233개 문구가 유지되는지 대조했다. 메모 HTML문자 escaping, 메모 저장/재접속, 기존 전체 객체 overlay 보존도 확인했다.
- 라이브: registry47개·이미지94개 응답과 파일 유형, index v48, SW v48 확인.
- 라이브 Chrome 추가 확인: **데드버그390px**, **드라이버320px**에서 이미지2컷·해당 id의 실제3D·메모 영역, 가로 넘침 없음, JS 오류 없음.
- 로컬 최종 스크린샷: `C:\Users\peopl\Documents\PT-GOLF-samples\model-review-final`.
- 라이브 스크린샷: 같은 루트의 `live-final-pt_deadbug.png`, `live-final-golf_driver.png`.
- 라이브 정적 자산 검증용 로컬 스크립트: `verify-all-live.cjs`. 앱 커밋5873b97/v48 값이 고정돼 있으므로 다음 릴리스에서 그대로 재사용하며 새 버전 검증이라고 말하지 않는다.
- 검증은 모델·UI의 구현과 데이터 보존을 확인한 것이다. 실제 모든 기기나 개인 운동 자세를 검증했다는 의미는 아니다.

## 9. 다음 세션 이월 사항 — 지금 추가 작업하지 않음

### 사용자 요청으로 남긴 우선 확인

**케이블 푸시다운 pt_pushdown**의 8/21 녹취 마지막41~44분이 깨져 다음 값이 미확인이다.

- 중량 kg
- 손잡이 종류: 스트레이트 바 / V바 / 로프
- 상체 각도
- 반복수·세트

다음 해당 녹취/사용자 답변에서 확인한 값을 spec·prep·큐·필요한 그림/3D에 반영하고 미확인 리마인더를 해소한다. 지금 뷰어의 스트레이트 바 예시는 당시 실제 세팅이 확인됐다는 뜻이 아니다. 다음 녹취에 없으면 빈 값을 임의로 채우지 않는다.

### 선택 후보·보류

- 운동 후 사용자 피드백에 따라 해당2컷/3D만 교정한다. 전체 디자인 일괄 적용은 끝났다.
- 실제 수행 녹취가 오면 신규 후보: 덤벨 프레스, 장요근/요방형근/대퇴직근 스트레칭, 불가리안 스플릿 스쿼트. 예고만으로 등록하지 않는다.
- 최근 일주일 화면은 이미 구현됐다. 과거의 “날짜별 선택 기능 추가”를 미완료로 오해하지 않는다. 별도 수행 이력 관리가 필요하면 사용자 요청 후 설계한다.
- 유튜브 쇼츠 https://www.youtube.com/shorts/2ISsMpTDMro 의 “한 화면·50단어 요약”은 사용자가 나중으로 미뤘다. 이 세션에서 재개하지 않았다.
- 옛 root HANDOFF/PDF/PNG/SVG/샘플 정리는 선택 사항이다. 미참조라는 이유로 사용자 자료를 일괄 삭제하지 않는다.
- 이번 핸드오프 작성 외에 후속 예약·자동 모니터링·반복 작업을 새로 만들지 않는다.

## 10. 다음 에이전트가 첫 응답 전에 확인할 것

1. 실제 루트에서 `git status -sb`, `git log -3 --oneline`으로 사용자 후속 변경 유무 확인.
2. 현재 사용자 요청이 녹취 반영인지, 특정 그림/3D 교정인지, 기능 수정인지 구분.
3. `data/seed.json`의 해당 운동과 `js/exercise-media.js`의 같은 id를 읽고, 9절의 미확인 세팅 확인.
4. 코칭이 바뀌면 그림/3D도 모순이 없는지 확인. 외형만 수정하면 운동 날짜를 건드리지 않기.
5. 자산 변경은 6절의 모든 버전 경로를 확인하고, 변경에 맞는 테스트만 실행.
6. 원문과 개인 메모를 보존한 상태로 commit/push하고, 실제 라이브 로딩 확인 후 완료 보고.
7. 핸드오프의 현재 상태·검증·새 이월 사항을 갱신하고, 자신이 띄운 미리보기 프로세스만 정리.

## 11. 주요 결정의 시간 순서

- 초기: GitHub Pages/PWA 정비, seed+localStorage 구조, 재활4동작과 SVG.
- 7월: 녹취별 PT 보강, 카테고리 정리, 이미지 확대, 칩 줄바꿈, 글자125%, 내보내기/가져오기 UI 제거.
- 8/6 전후: 검색 보강, 전 운동 focus 개념 카드, 골프 체중이동·지연·면 임팩트 핵심 정리.
- **8/11 트레이너 변경**: 이상근 벤치 버전으로 대체. 동작마다 대체/보강 판정.
- 8/18~8/21: 데드버그40% 강조, 머신 로우·보수 로테이션·레그 프레스, 케이블 푸시다운 및 미확인 세팅 이월.
- 8/27: 등장 운동 전면 대체 요청, 백 익스텐션 신규.
- 이후: 최근7일 날짜 선택, 어깨 카테고리, 9/4 준비/핵심/수치 구조, 고블릿 명칭 확정, 추천운동→코어·밸런스.
- 9/10: 체스트 프레스·펙덱 추가, 레그 익스텐션/와이드 스쿼트 교정. seed33, 47운동·10원칙.
- 9/11~9/12: 정적2컷·GIF/3D 샘플 검토를 거쳐 회전형3D 채택. 위치를 **2컷과 움직임·느낌 사이**로 확정.
- **2218a78**: 케이블 푸시다운 완성본, 원문·메모 유지, 불필요한 확대/소개 문구 제거. 자산47.
- **5873b97**: 나머지46개에도2컷+3D 적용, 전체검증·모바일배포 완료. 자산48, seed33 유지.
- 이번 종료 요청: 이 문서를 최신 자기완결형 기준으로 재작성. 앱 코드는 추가 변경하지 않음.
